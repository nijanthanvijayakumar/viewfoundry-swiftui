import SwiftUI

struct ViewFoundryGeneratedView: View {
    var body: some View {
        VStack(alignment: .center, spacing: 10) {
            Text("Unsupported request parts")
                .font(.headline.bold())

            Text("Rendered with static fallback copy.")
                .font(.body)
                .foregroundStyle(.secondary)
        }
        .padding(18)
    }
}
